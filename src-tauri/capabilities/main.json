{
  "identifier": "main",
  "description": "Main window permissions for dialog access, platform detection, and transfer commands.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "os:default",
    "allow-app-commands"
  ]
}
